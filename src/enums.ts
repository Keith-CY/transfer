export enum FileErrors {
  NotFound = 'File Not Found',
  Exist = 'File Exists',
}

export enum FileAction {
  CREATE = 'create',
  UPDATE = 'update',
}

export enum ForceFlag {
  NO = '0',
  YES = '1',
}
